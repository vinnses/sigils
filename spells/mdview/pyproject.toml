[project]
name = "mdview-spell"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "markdown-it-py",
    "mdit-py-plugins",
    "pygments",
    "PyYAML",
    "watchfiles",
]

[project.optional-dependencies]
dev = ["pytest"]

[dependency-groups]
dev = ["pytest"]
